'use strict';
const output = document.getElementById('output');

// функция запроса
const getData = () => {

    // создаем запрос
    const request = new XMLHttpRequest();
    // настраиваем его
    request.open();
};